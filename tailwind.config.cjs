/**
 * Minimal Tailwind config to enable scanning HTML and JS in the repo.
 */
module.exports = {
  content: [
    './**/*.html',
    './**/*.js'
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
